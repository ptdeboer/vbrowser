###
# File      : build.prop
# Component : vlet-gui.vbrowser
# 

###
# Do not add spaces to the end of the values ! 
# Component defaults (not that component.root is set in the main build file)
comp.groupId=nl.nlesc.vlet
comp.artifactId=vlet-gui.vbrowser
comp.dist=${comp.root}/dist
comp.build=${comp.root}/build
comp.src=${comp.root}/src

# needed for manifest (should define for all components at toplevel build) 
comp.version=${TODAY}
comp.vendor=vlet

